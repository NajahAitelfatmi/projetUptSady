pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Exemple de commande de build
                sh 'mvn clean install' // Commande pour Maven, adaptez selon vos besoins
            }
        }
    }
}

